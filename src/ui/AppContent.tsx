import { useImageLoader } from '../services/images/context';

export function AppContent() {
  const imageLoader = useImageLoader();

  //return (
    //<YourUIComponent imageLoader={imageLoader} />    // Replace YourUIComponent in the future 
  //);
}

